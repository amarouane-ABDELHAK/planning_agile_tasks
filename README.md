# HOW To
```sh
$cp env.sh.git env.sh
$# Add your jira username/password to env.sh
$# Define the project you want to follow in plant.rb file $jira_project ='XXXZ'
$docker-compose up
$# Go to http://localhost:3004/ 
```

